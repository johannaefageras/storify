name: Keep Supabase alive

on:
  schedule:
    - cron: '0 0 */5 * *' # Every 5 days

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: curl -sf "${{ secrets.PUBLIC_SUPABASE_URL }}/rest/v1/" -H "apikey: ${{ secrets.PUBLIC_SUPABASE_ANON_KEY }}" -o /dev/null
